# Terraform working directory and state cache
.terraform/
.terraform.lock.hcl

# State files — never commit these (state is stored in S3)
terraform.tfstate
terraform.tfstate.backup

# Variable files — may contain secrets
*.tfvars
*.tfvars.json

# Lambda deployment zip (generated by CI or bootstrap script)
backend.zip
